# 本地 EMQX（可选）

```powershell
cd iot/emqx
docker compose up -d
```

控制台默认：`http://127.0.0.1:18083`（账号密码以镜像文档为准）。

Webhook 与 Topic 规范见 [docs/runbooks/EMQX_阶段A_联调配置模板.md](../../docs/runbooks/EMQX_阶段A_联调配置模板.md)。
